// Positron — Datenbank-Konstanten

/** Umgebungsvariable für benutzerdefinierten DB-Pfad */
export const POSITRON_DB_PATH = 'POSITRON_DB_PATH';

/** Timeout für SQLite-Operationen in ms */
export const DB_TIMEOUT_MS = 5000;
